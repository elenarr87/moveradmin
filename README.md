# Mover Admin

Малък администратор/редактор за статичен HTML сайт (Mover Studio).

## Какво прави
- Позволява редакция на текст и качване на изображения чрез безопасни модали (без contenteditable).
- Запазва съдържанието в `content/index.json` (когато `.mover/save.php` работи) или локално в браузъра (`localStorage` ключ `mover_content_backup`) за тестове.

## Важни файлове
- `index.html` — главна страница + шаблони за секции
- `mover-admin.js` — клиентска логика (редактирай/запази/качвай)
- `mover-admin.css` — стилове за админ UI
- `.mover/save.php` — endpoint за запис на `content/index.json` (трябва да работи на хостинга)
- `.mover/upload.php` — endpoint за качване на изображения (трябва да връща публичен URL)
- `content/index.json` — canonical съдържанието на сайта

## Бързо пускане и тестване (локално)
1. В project директория стартирайте PHP вграден сървър:
```powershell
php -S localhost:8000 -t .
```
2. Отворете: `http://localhost:8000/index.html#admin` и влезте с парола (auth handled by `.mover/auth.php`).
3. Редактирайте герой/текст/секции → натиснете `💾 Save All Changes`.
4. В конзолата на браузъра проверете:
```js
localStorage.getItem('mover_content_backup')
```
Ако server не е наличен, промените ще бъдат записани само локално.

## Разполагане на хостинг
- Качете всички файлове в корена на сайта (включително `.mover` и `content`).
- Уверете се, че `.mover/save.php` и `.mover/upload.php` могат да записват файлове на диска.
- Дайте нужните права на `content/` и папката за качени изображения (пример):
```bash
chown -R www-data:www-data content assets/uploads
chmod -R 775 content assets/uploads
```
(заменете `www-data` с потребителя на вашия web/PHP процес)

## Отстраняване на проблеми
- Ако `Save All` не актуализира `content/index.json` — проверете Network таба за POST към `.mover/save.php` и отговора (трябва да върне `{"ok":true}`).
- Ако качените изображения не са достъпни — проверете отговора от `.mover/upload.php` (трябва да върне `ok:true` и `url`) и правата/пътищата на папката.
- За бързо локално тестване без PHP: скриптът използва `localStorage` като fallback (ключ `mover_content_backup`). Това е само за тест и не синхронизира между потребители.

## Следващи стъпки (предложения)
- Премахване на debug-alerts/console.log преди прод. ползване.
- Преглед/поправка на `.mover/save.php` и `.mover/upload.php`, ако желаете — мога да помогна.

---
Ако искаш, мога да добавя команди за deploy, лиценз или контакти.
# Mover Studio Admin

A micro PHP backend for managing clean HTML websites.

## Setup

1. Upload all files to your web server with PHP support.
2. Visit `/.mover/setup.php` to generate a password hash.
3. Replace the `$correct_hash` in `.mover/auth.php` with your generated hash.
4. Delete `setup.php`.
5. Access admin by visiting `yoursite.com/#admin` and enter the password.

## Features

- Clean HTML frontend
- JSON-based content storage
- Image uploads
- Session-based authentication
- No dependencies

## Default Password

The default hash is for password 'password'. Change it using setup.php.